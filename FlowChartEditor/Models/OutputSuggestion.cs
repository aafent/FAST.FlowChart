namespace FlowChartEditor.Models;

/// <summary>
/// Represents a suggestion entry for the EndTerminal Outputs value dropdown.
/// Color is blue for Arguments, black for Variables.
/// </summary>
public record OutputSuggestion(string Value, string Color);
